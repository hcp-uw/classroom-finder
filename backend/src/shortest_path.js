// aStar(graph, heuristic, start, goal) {

// };

function h(state) {
	// let x = Math.abs(state.x - goal.x);
	// let y = Math.abd(state.y - goal.y);
	// let distance = Math.sqrt((x * x) + (y * y));
	// return distance;
}
